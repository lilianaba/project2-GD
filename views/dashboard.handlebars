{{#each bars as |bar|}}
  <section>
    <div>
      <div class="barList">
        <a href="/dashboard/bar/{{bar.id}}">{{bar.name}}</a>
        <p>Bar rating: {{bar.ratingAvg}}</p>
      </div>
    </div>
  </section>
{{/each}}
